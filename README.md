# Trading DNA

Standalone build of the Trading DNA app from aschcapital.com.

Extracted from the aschcapital.com single-page site into a self-contained app: a minimal shell (`index.html`) boots the labs runtime directly into the `trading-dna` screen. Open `index.html` in a browser (or serve the folder) to run it.
